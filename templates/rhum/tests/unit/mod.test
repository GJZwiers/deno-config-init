import { Rhum } from "../../dev_deps.{{extension}}";
import { greet } from "../../mod.{{extension}}"

Rhum.testSuite("mod.ts", () => {
    Rhum.testCase("Returns a greeting", () => {
        Rhum.asserts.assertEquals(greet(), "Hello World!");
    });
})

Rhum.run();
