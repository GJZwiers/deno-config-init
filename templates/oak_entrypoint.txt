import { Application, Context } from "./deps.{{extension}}";

const app = new Application();

app.use((context{{type:Context}}) => {
  context.response.body = "Hello world!";
});

await app.listen("127.0.0.1:8000");